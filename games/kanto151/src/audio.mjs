// Original synthesized score and sound design. No sampled game audio.
export class AudioDirector{
 constructor(){this.ctx=null;this.volume=.4;this.mode='menu';this.beat=0;this.last=0;}
 start(){if(!this.ctx){this.ctx=new (window.AudioContext||window.webkitAudioContext)();this.master=this.ctx.createGain();this.master.gain.value=this.volume;const compressor=this.ctx.createDynamicsCompressor();this.master.connect(compressor);compressor.connect(this.ctx.destination);}if(this.ctx.state==='suspended')this.ctx.resume();}
 setVolume(v){this.volume=v;if(this.master)this.master.gain.setTargetAtTime(v,this.ctx.currentTime,.1);}
 combat(stage,type,tier='medium',pan=0){if(!this.ctx||!this.volume)return;const c=this.ctx,t=c.currentTime;if(stage==='impact'&&t-(this.lastImpactTime||0)<.045)return;if(stage==='impact')this.lastImpactTime=t;const gain={light:.035,medium:.05,heavy:.066,signature:.075}[tier]||.05;
  // Eight reusable panners bound simultaneous voices; short oscillators/buffer sources
  // are one-shot WebAudio nodes and disconnect on completion.
  if(!this.emitters){this.emitters=Array.from({length:8},()=>{const p=c.createStereoPanner();p.connect(this.master);return p;});const b=c.createBuffer(1,c.sampleRate,c.sampleRate),data=b.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=Math.random()*2-1;this.noiseBuffer=b;this.voiceIndex=0;}
  const bus=this.emitters[this.voiceIndex++%8];bus.pan.setTargetAtTime(pan,t,.015);
  const tone=(hz,end,duration,shape='sine',volume=gain,delay=0)=>{if((this.activeCombatVoices||0)>=48)return;this.activeCombatVoices=(this.activeCombatVoices||0)+1;const o=c.createOscillator(),g=c.createGain();o.type=shape;o.frequency.setValueAtTime(hz,t+delay);o.frequency.exponentialRampToValueAtTime(Math.max(25,end),t+delay+duration);g.gain.setValueAtTime(.0001,t+delay);g.gain.exponentialRampToValueAtTime(volume,t+delay+.008);g.gain.exponentialRampToValueAtTime(.0001,t+delay+duration);o.connect(g);g.connect(bus);o.start(t+delay);o.stop(t+delay+duration+.02);o.onended=()=>{this.activeCombatVoices--;o.disconnect();g.disconnect();};};
  const air=(freq,duration,volume=gain)=>{if((this.activeCombatVoices||0)>=48)return;this.activeCombatVoices=(this.activeCombatVoices||0)+1;const n=c.createBufferSource(),f=c.createBiquadFilter(),g=c.createGain();n.buffer=this.noiseBuffer;f.type='bandpass';f.frequency.value=freq;f.Q.value=type==='water'?.6:1.4;g.gain.setValueAtTime(volume,t);g.gain.exponentialRampToValueAtTime(.0001,t+duration);n.connect(f);f.connect(g);g.connect(bus);n.start(t);n.stop(t+duration);n.onended=()=>{this.activeCombatVoices--;n.disconnect();f.disconnect();g.disconnect();};};
  if(stage==='travel'){air({electric:4200,fire:850,water:1800,ghost:340,psychic:2200}[type]||1600,.22,gain*.45);tone({ghost:160,psychic:620,water:430}[type]||280,160,.2,'sine',gain*.25);return;}if(stage==='launch'){const hz={electric:950,fire:150,water:320,grass:580,ice:1700,psychic:480,ghost:145,ground:80,rock:115,flying:900,bug:1300,poison:220,dragon:175}[type]||240;tone(hz,hz*1.4,.14,'triangle',gain*.65);air(type==='fire'?700:2400,.15,gain*.5);return;}
  tone(105,42,.12,'triangle',gain); // body/contact, separate from elemental resonance
  const layers={electric:()=>{air(4100,.095,gain*1.6);tone(720,105,.22,'sawtooth',gain*.55);},fire:()=>{air(650,.42,gain*1.5);tone(95,35,.32,'triangle');},water:()=>{air(1700,.34,gain*1.5);tone(430,130,.27);tone(950,330,.14,'sine',gain*.45,.08);},grass:()=>{air(3600,.16);tone(520,270,.14,'triangle');},ice:()=>{[1800,2350,2900].forEach((f,i)=>tone(f,f*.7,.3,'sine',gain*.6,i*.025));},psychic:()=>{tone(260,160,.6);tone(530,540,.5,'sine',gain*.55);},ghost:()=>{tone(145,65,.65,'triangle');tone(640,140,.45,'sine',gain*.55,.08);},ground:()=>{air(220,.35,gain*1.8);tone(70,30,.35);},rock:()=>{air(900,.12,gain*1.5);tone(160,55,.2,'triangle');},flying:()=>air(3300,.35,gain*1.3),bug:()=>{[0,.045,.09].forEach(d=>tone(1050,600,.06,'triangle',gain,d));},poison:()=>{tone(240,80,.3,'sine');tone(330,110,.18,'sine',gain,.12);},dragon:()=>{tone(85,40,.45,'sawtooth',gain*.55);tone(390,780,.35);air(1200,.38);}};
  (layers[type]||(()=>air(1900,.09,gain*1.4)))();tone(360,180,.09,'sine',gain*.22,.025);
  if(['heavy','signature'].includes(tier))tone(tier==='signature'?58:72,30,.3,'sine',gain*1.4);
  this.lastCombat={stage,type,tier,pan,layers:['physical','elemental','reaction',...(['heavy','signature'].includes(tier)?['bass']:[])]};
 }
 note(f,d=.2,type='sine',gain=.08,delay=0,slide=1){if(!this.ctx||!this.volume)return;const t=this.ctx.currentTime+delay,o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.setValueAtTime(f,t);o.frequency.exponentialRampToValueAtTime(Math.max(20,f*slide),t+d);g.gain.setValueAtTime(0,t);g.gain.linearRampToValueAtTime(gain,t+.015);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g);g.connect(this.master);o.start(t);o.stop(t+d+.05);o.onended=()=>{o.disconnect();g.disconnect();};}
 noise(d=.2,gain=.05,filter=1500){if(!this.ctx||!this.volume)return;const c=this.ctx,b=c.createBuffer(1,Math.ceil(c.sampleRate*d),c.sampleRate),a=b.getChannelData(0);for(let i=0;i<a.length;i++)a[i]=(Math.random()*2-1)*(1-i/a.length);const n=c.createBufferSource(),f=c.createBiquadFilter(),g=c.createGain();n.buffer=b;f.type='lowpass';f.frequency.value=filter;g.gain.value=gain;n.connect(f);f.connect(g);g.connect(this.master);n.start();n.onended=()=>{n.disconnect();f.disconnect();g.disconnect();};}
 play(kind,type='normal'){if(!this.ctx)return;if(['success','register','evolution','level'].includes(kind)){[0,4,7,12,16].forEach((n,i)=>this.note(261.63*2**(n/12),.7,'sine',.16,i*.105));return;}
 if(kind==='move'){const base={electric:880,fire:125,water:350,grass:620,psychic:720,ghost:155,ice:1200,dragon:180}[type]||260;this.note(base,.32,['electric','ghost'].includes(type)?'sawtooth':'triangle',.14,0,type==='water'?2.1:.45);this.note(base*1.5,.45,'sine',.1,.03,.7);this.noise(.25,.12,type==='fire'?800:3800);return;}
 if(kind==='hit'){this.noise(.13,.18,1100);this.note(95,.12,'triangle',.15,0,.35);}else if(kind==='throw'){this.noise(.22,.07,4200);this.note(420,.3,'sine',.12,0,1.8);}else if(kind==='shake')this.note(590,.13,'sine',.15,0,.8);else if(kind==='fail'){this.note(230,.4,'triangle',.12,0,.5);}else if(kind==='boss'){[0,1,7].forEach((n,i)=>this.note(65*2**(n/12),2.5,'sawtooth',.08,i*.18));}else if(kind==='dodge')this.noise(.2,.08,2500);else this.note(780,.09,'sine',.08);}
 update(dt,mode,biome='camp'){this.mode=mode;this.last-=dt;if(this.last>0||!this.ctx||this.ctx.state!=='running')return;this.last=mode==='boss'?.27:mode==='battle'?.34:.55;const scales={menu:[0,7,12,14,16,19,16,12],field:[0,4,7,11,14,7,4,2],battle:[0,3,7,10,12,10,7,3],boss:[0,1,7,8,12,8,7,1]};const seq=scales[mode]||scales.field,minor=['cave','ruins','laboratory'].includes(biome),n=seq[this.beat%seq.length];this.note((minor?196:220)*2**(n/12),1.1,'sine',.045);if(this.beat%4===0)this.note((minor?49:55)*2**((Math.floor(this.beat/16)%2)*5/12),2,'triangle',.07);if(mode==='boss'||mode==='battle')this.noise(.06,.06,500);else if(this.beat%7===0){const ambience={camp:620,grassland:1450,forest:1900,lake:350,coast:260,mountain:850,cave:120,wetland:480,powerplant:55,ruins:155,safari:1100,volcano:75,frozen:1300,laboratory:220}[biome]||400;this.note(ambience,1.5,'sine',.012,0,['volcano','ruins','cave'].includes(biome)?.7:1.08);if(['camp','grassland','forest','safari'].includes(biome)){this.note(1700,.12,'sine',.017,.2,1.3);this.note(2000,.1,'sine',.014,.36,.8);this.noise(1.2,.009,2900);}else if(['lake','coast','wetland'].includes(biome)){this.noise(biome==='coast'?1.8:.9,.014,biome==='wetland'?550:1400);this.note(biome==='wetland'?280:780,.2,'sine',.008,.25,.6);}else if(['powerplant','laboratory'].includes(biome)){this.note(55,1.4,'triangle',.016);this.note(1400,.4,'sine',.008,.3,.97);}else if(biome==='volcano'){this.noise(1.4,.02,200);this.note(48,1.5,'triangle',.016);}else if(biome==='frozen'){this.noise(1.4,.009,3200);this.note(2100,.8,'sine',.008,.15,.92);}else {this.noise(1.2,.012,biome==='cave'?500:2100);this.note(720,.4,'sine',.009,.45,.95);}}this.beat++;}
}

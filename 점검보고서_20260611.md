# TCG Hub 점검 보고서

작성일: 2026-06-11 · 대상: index.html(9,454줄), functions/(API 40여 개), sw.js, 배포 설정

---

## 요약

심각한 보안 구멍 1건(저장형 XSS), 운영 사고 위험 1건(수동 배포 시 쿠키·시크릿 유출), 그리고 성능을 크게 끌어올릴 수 있는 지점 몇 곳이 핵심입니다. 코드 자체 품질은 전반적으로 양호하고 인증·rate limit·이미지 프록시 화이트리스트 등 기본기는 잘 잡혀 있습니다.

심각도 표기: 🔴 높음 / 🟡 중간 / 🟢 낮음(개선)

---

## 🔴 높음 — 우선 처리

### 1. 저장형 XSS — 관리자 글 본문이 그대로 innerHTML 삽입
`renderRichContent()` (index.html:7033) 가 HTML 태그가 포함된 본문을 **검증 없이 그대로 반환**하고, 이게 뉴스/이벤트/카드쇼 카드(7059, 7196, 7327행)와 Quill 뷰어(7809행 `_QUILL.root.innerHTML = e.content`)에 들어갑니다.

```js
function renderRichContent(s) {
  if (/<[a-z][^>]*>/i.test(s)) return s;   // ← 무방비
  return nl2br(s);
}
```

`/api/admin` 은 비밀번호로 쓰기를 막고 있어 "관리자만 작성"이 전제지만, 본문에 `<img onerror>` `<script>` 류가 들어가면 **모든 방문자 브라우저에서 실행**됩니다. 관리자 계정/비번이 새거나 KV가 다른 경로로 오염되면 전체 사용자 대상 공격이 됩니다. 사이트에 DOMPurify가 전혀 없습니다(검색 결과 0건).

수정안: DOMPurify를 도입하거나(권장), 허용 태그/속성 화이트리스트 기반 sanitizer를 `renderRichContent`와 Quill 주입 지점에 적용. `on*` 속성·`<script>`·`javascript:` URL 제거.

### 2. 수동 배포 시 시크릿·쿠키 유출 위험
`배포_wrangler.bat` 이 `wrangler pages deploy .` 로 **폴더 전체**를 올립니다. 이 폴더에는 `cookies.json`(SNKRDUNK 세션 쿠키 실값), `index.OLD-backup.html`, `index.html.bak`, `TODO_TOMORROW.md`, `backfill_op16.log`, `data/_bak-op16-*`(42MB 백업 3벌), `debug/` 가 들어 있습니다.

`.gitignore` 로 git 커밋은 막혀 있지만 **wrangler 직접 배포는 .gitignore를 따르지 않습니다.** GitHub Actions 배포(scrape.yml)는 `_deploy/`에 필요한 파일만 골라 담아 안전하지만, .bat을 한 번이라도 수동 실행하면 `https://tcghub.kr/cookies.json` 으로 쿠키가 공개될 수 있습니다.

> 참고: 현재 운영 사이트에서 해당 경로들을 확인하니 실제 파일 대신 SPA(index.html)가 반환됩니다 — 즉 지금 라이브에는 노출돼 있지 않습니다. 하지만 .bat 수동 배포 한 번이면 바로 뚫리는 구조입니다.

수정안: `cookies.json` 같은 시크릿은 환경변수/KV로 이동. 배포 시 `.assetsignore` 파일을 두거나, .bat도 `_deploy/` 선별 복사 방식으로 통일. 백업 파일들(`*.bak`, `*.OLD`, `_bak-*`, `debug/`, 로그)은 배포 대상에서 제외.

---

## 🟡 중간

### 3. 초기 로딩 무게 — all-cards.json 9MB
시세 페이지 진입 시 `loadCardSearchData()` 가 `all-cards.json`(9.0MB, gzip 후에도 약 827KB, 23,102개 카드)을 통째로 받아 메모리에 인덱싱합니다. 같은 데이터가 `window._ALL_CARDS_DETAILS` 로도 중복 보관됩니다(2956, 4853행).

개선안: 검색 인덱스는 이름·번호·id·썸네일만 담은 경량 파일로 분리(예: `search-index.json`, 1~2MB), 상세는 카드 클릭 시 `card?id=` API로 lazy load. 또는 서버에서 검색을 처리. 단기로는 `_ALL_CARDS_INDEX` 와 `_ALL_CARDS_DETAILS` 중복 제거만 해도 메모리 절감.

### 4. 전역 에러 핸들러 부재
`window.onerror` / `unhandledrejection` 핸들러가 없습니다(검색 0건). 9천 줄 SPA에서 한 곳의 예외가 조용히 죽으면 페이지 일부가 멈춰도 감지가 안 됩니다.

개선안: 전역 핸들러를 추가해 최소한 콘솔/사용자 토스트로 알리거나 경량 로깅 엔드포인트로 전송.

### 5. 미사용·중복 파일 정리
`index.html`(497KB)과 거의 같은 `index.OLD-backup.html`(232KB), `index.html.bak`(413KB), `mobile.html`(예전 모바일 버전?), `data/_bak-op16-*` 3벌(각 14MB), `cookies.json`, 다수의 `data/_*.json`(_suspicious_matches, _pending, _template 등)이 리포에 남아 있습니다. `index.OLD-backup.html` 은 git에 커밋까지 되어 있습니다.

개선안: 백업·임시 파일을 별도 보관 위치로 옮기고 리포·배포 대상에서 제거.

### 6. 외부 스크립트 버전 미고정
`lucide@latest`(147행)를 latest로 로드 — 업스트림 변경 시 깨질 수 있고 캐싱·SRI도 어렵습니다. AdSense·Quill·Sortable은 OK.

개선안: lucide도 특정 버전으로 핀 고정.

---

## 🟢 낮음 / 개선

- **viewport `user-scalable=no, maximum-scale=1.0`** (6행): 확대 차단은 접근성 위배 + iOS Safari 무시. 제거 권장.
- **`localStorage` 미가드 파싱**: `JSON.parse(localStorage.getItem('aiCenteringCount')||'{}')` (3157행) 는 `'{}'` 폴백이 있어 안전하나, 손상된 값이 들어오면 throw. try/catch 권장.
- **캐러셀 setInterval 정리 확인**: `_ETC_CAROUSEL_TIMER`/`_EVENTS_CAROUSEL_TIMER`(2594, 2683행) 재생성 전 clearInterval 되는지 확인 — 페이지 재진입 시 타이머 누적 가능성.
- **centering-ai 요청 본문 크기 제한 없음**: `frontImage`/`backImage`(base64)에 바이트 상한이 없습니다. 일 3회 제한·로그인 요구는 있으나, 거대한 페이로드 방어용 상한(예: 8MB) 추가 권장.
- **`escape()`/`unescape()` deprecated** (admin.js:54, base64 디코딩): 동작은 하나 `decodeURIComponent`/`TextDecoder` 기반으로 교체 권장.
- **SEO 중복 메타**: `<meta name="description">`·OG 태그가 head에 두 번씩 정의돼 있습니다(10·135행 등). 크롤러는 보통 첫 번째를 채택하나 정리하면 깔끔.
- **sw.js 캐시 안전성**: 전략 자체는 합리적(api network-only, data SWR, 이미지 cache-first). `CACHE_VERSION` 수동 관리이므로 배포 시 버전 갱신 잊지 않기.

---

## 잘 되어 있는 점
- 시세/뉴스 카드의 동적 값은 `escapeHtml()` 로 일관되게 이스케이프 (#1의 본문만 예외).
- 이미지 프록시 2종 모두 호스트 화이트리스트 적용.
- JWT HS256 직접 구현 정상, 세션 쿠키 `HttpOnly; Secure; SameSite=Lax`.
- 매직링크 토큰 1회용 처리 + race condition 방지(UPDATE ... WHERE used=0), 1분당 5회 rate limit.
- _headers 의 index.html/sw.js no-store 캐시 정책 적절.

---

## 권장 처리 순서
1. **#1 XSS** — sanitizer 도입 (가장 시급)
2. **#2 배포 유출** — .bat 선별 배포로 전환 + cookies.json 시크릿화
3. **#5 파일 정리** + **#3 로딩 경량화**
4. 나머지 🟡/🟢 일괄
